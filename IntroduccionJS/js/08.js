// Template strings o Concatenación

const client = "Juan D"
const wallet = 400

console.log(`El cliente : ${client} tiene un saldo de: ${wallet}`)

const product = {
    name : "tablet",
    price : 300,
    avaible : true
}

console.log(`El producto: ${product.name}, tiene un precio de: ${product.price}`)